schema_version = "1.0"

id = "rails-adoption"
name = "Rails adoption"
status = "implemented"
owner = "docs-platform"

objective = """
Adopt .rails/ as the durable framework root and establish the initial artifact graph.
"""

end_state = [
  ".rails/ footprint and docs are in place.",
  "Foundational proposal/spec/ADR are linked in index.",
  "Rails registry validation is available through cargo +1.95.0 run -p xtask -- rails check.",
]

[[work_item]]
id = "rails-adoption-foundation"
status = "implemented"
proposal = "PERFGATE-PROP-0001"
spec = "PERFGATE-SPEC-0001"
adr = "PERFGATE-ADR-0001"
implementation_plan = ".rails/lanes/rails-adoption/implementation-plan.md"
blocks = []
blocked_by = []
proof = [
  "git diff --check",
  "cargo +1.95.0 run -p xtask -- rails check",
]
