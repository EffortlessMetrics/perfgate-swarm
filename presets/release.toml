# Release preset: high accuracy with tight threshold.
# Suitable for release branches and nightly checks.

[defaults]
repeat = 10
warmup = 2
threshold = 0.10
warn_factor = 0.50
noise_threshold = 0.08
noise_policy = "warn"
out_dir = "artifacts/perfgate"
baseline_dir = "baselines"
