name: mutation-testing

on:
  # Run weekly on Sunday at midnight UTC
  schedule:
    - cron: "0 0 * * 0"
  # Allow manual triggering
  workflow_dispatch:
    inputs:
      crate:
        description: "Specific crate to test, or 'all' for all crates"
        required: false
        type: choice
        default: all
        options:
          - all
          - perfgate-domain
          - perfgate-types
          - perfgate-app
          - perfgate-cli
          - perfgate-paired
          - perfgate-fake

jobs:
  mutation-test:
    runs-on: ubuntu-latest
    timeout-minutes: 120 # Mutation testing can take a while

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-mutants
        run: cargo install cargo-mutants

      - name: Run mutation testing
        env:
          MUTATION_CRATE: ${{ inputs.crate || 'all' }}
        run: |
          if [ "${MUTATION_CRATE}" != "all" ]; then
            cargo run -p xtask -- mutants --crate "${MUTATION_CRATE}" --summary
          else
            cargo run -p xtask -- mutants --summary
          fi

      - name: Upload mutation report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mutation-report
          path: |
            mutants.out/
          retention-days: 30

      - name: Generate summary
        if: always()
        run: |
          echo "## Mutation Testing Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ -f mutants.out/outcomes.json ]; then
            # Parse outcomes and generate summary
            TOTAL=$(jq 'length' mutants.out/outcomes.json)
            KILLED=$(jq '[.[] | select(.summary == "CaughtMutant")] | length' mutants.out/outcomes.json)
            SURVIVED=$(jq '[.[] | select(.summary == "MissedMutant")] | length' mutants.out/outcomes.json)
            TIMEOUT=$(jq '[.[] | select(.summary == "Timeout")] | length' mutants.out/outcomes.json)
            
            if [ "$TOTAL" -gt 0 ]; then
              KILL_RATE=$(echo "scale=1; $KILLED * 100 / $TOTAL" | bc)
            else
              KILL_RATE="N/A"
            fi
            
            echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| Total Mutants | $TOTAL |" >> $GITHUB_STEP_SUMMARY
            echo "| Killed | $KILLED |" >> $GITHUB_STEP_SUMMARY
            echo "| Survived | $SURVIVED |" >> $GITHUB_STEP_SUMMARY
            echo "| Timeout | $TIMEOUT |" >> $GITHUB_STEP_SUMMARY
            echo "| **Kill Rate** | **${KILL_RATE}%** |" >> $GITHUB_STEP_SUMMARY
            
            if [ "$SURVIVED" -gt 0 ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "### Surviving Mutants" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "See the uploaded artifact for details on surviving mutants." >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "No mutation testing results found." >> $GITHUB_STEP_SUMMARY
          fi
